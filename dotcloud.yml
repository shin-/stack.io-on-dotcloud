server:
    type: custom
    ports:
        www: http
    buildscript: build.sh
    processes:
        server: ./start.sh index
        echo: ./start.sh echo-service
    environment:
        REGISTRAR_ENDPOINT: tcp://0.0.0.0:27615
        ECHO_ENDPOINT: tcp://127.0.0.1:32222